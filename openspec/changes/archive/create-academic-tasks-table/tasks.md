# Tarefas: create-academic-tasks-table

- [ ] Criar a tabela `academic_tasks` com os seguintes campos:
  - `title`: `text`
  - `description`: `text`
  - `due_date`: `date`
  - `status`: `text` (com valor padrão "pendente")
  - `subject_id`: `integer` (referência à tabela `subjects`)
